package Core;

/**
 * This is really just a dummy class so that TimeSpan and scheduling can work out even though
 * the BPM changes.
 */
public class BPM {
    public int bpm;

    public BPM(int bpm) {
        this.bpm = bpm;
    }
}
